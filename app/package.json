{
  "name": "outgunned-tools",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:html": "vite build --mode singlefile && mkdir -p ../play && cp dist/index.html ../play/outgunned-tools.html",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": { "react": "^18.3.0", "react-dom": "^18.3.0", "pdf-lib": "^1.17.1" },
  "devDependencies": { "@vitejs/plugin-react": "^4.3.0", "vite": "^6.0.0", "vite-plugin-singlefile": "^2.0.0", "vitest": "^3.0.0" }
}
