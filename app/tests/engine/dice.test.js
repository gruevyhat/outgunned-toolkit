import {describe,expect,it} from 'vitest'; import {coin,d6,d66,makeRng,pick,shuffle} from '../../src/engine/dice.js'; import {roll,validateTable} from '../../src/engine/tables.js'
describe('dice',()=>{it('is deterministic and bounded',()=>{const a=makeRng(42),b=makeRng(42);expect(Array.from({length:20},()=>a())).toEqual(Array.from({length:20},()=>b()));const rng=makeRng(2);for(let i=0;i<100;i++){expect(d6(rng)).toBeGreaterThanOrEqual(1);expect(d6(rng)).toBeLessThanOrEqual(6);expect([0,1]).toContain(coin(rng));expect(d66(rng)).toBeGreaterThanOrEqual(11)}expect(pick(rng,['a'])).toBe('a');expect(shuffle(rng,[1,2,3]).sort()).toEqual([1,2,3])})})
describe('tables',()=>{it('validates exact die coverage and rolls',()=>{const t={die:'d6',columns:['value'],rows:Array.from({length:6},(_,i)=>({roll:i+1,value:`v${i+1}`}))};expect(validateTable(t)).toEqual([]);expect(roll(makeRng(1),t).row.value).toMatch(/^v/) });it('rejects gaps',()=>expect(validateTable({die:'d6',columns:['x'],rows:[{roll:'1-5',x:'x'}]})).toContain('Roll 6 is not covered exactly once'))})
